package com.example.tidegrab;

//Handles the storage of all data that needs to persist beyond the Application lifetime
public class DataStorage {
	private String dataSets = "res/raw/testfile.txt";
	
	public void writeDataSet(){
		//Must check if data already exists for that date and station
		//If it does, remove it and add in again
		//If it does not, add data in
	}
	
	
}
